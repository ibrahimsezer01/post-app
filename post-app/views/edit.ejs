<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <title>Edit Post</title>
</head>
<body>
    <!-- Navbar -->
    <%- include('./partials/header') %>

    <div class="container mt-4">
        <h1>Edit Post</h1>
        <!-- Form for editing the post -->
        <form method="POST" enctype="multipart/form-data">
            <div class="form-group">
                <label for="title">Title</label>
                <input type="text" class="form-control" id="title" name="title" placeholder="Title" value="<%= post.title %>" required>
            </div>
            <div class="form-group">
                <label for="content">Content</label>
                <textarea class="form-control" id="content" name="content" rows="5" placeholder="Content" required><%= post.content %></textarea>
            </div>
            <div class="form-group">
                <label for="image" class="form-label">İmage:</label><br>
                <img src="../public/post_images/<%= post.image %>" alt="" width="100px" height="100px" class="mb-3">
                <input type="file" id="image" name="image" class="form-control">
            </div>
            <button type="submit" class="btn btn-primary">Update Post</button>
        </form>
    </div>

    <!-- Bootstrap JS -->
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
